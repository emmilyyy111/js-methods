// removing the element on the top of the array/ stack or the last element added to the array

// method one 
const array = ['emily', 'jessica', 'max', 'jeremy']
const removeLastElementAdded = (arr) => {
    for (let i = 0; i <= array.length; i++)  {
            arr.push('jes')
    }
    return arr
    
};
// console.log(removeLastElementAdded(array));

let Friends = [ "20", "21", "27", "24"];
let newFriends = Friends.pop();
console.log(newFriends);