const team = ['emily', 'taylor', 'ava', 'kenza', 'nickPAPA'];
const response = ['try again', 'yes deff', 'hell nah', 'cannot answer at this moment'];


function getAllTeamMembers (response) {
    for (let i = 0; i<response.length; i++){
        return response[3]
    }
}
console.log(getAllTeamMembers(response));